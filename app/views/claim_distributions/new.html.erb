
<%# <div class="card">
  <h5 class="card-header">Edit claim distribution</h5>
  <div class="card-body"> %>
    <%#= render "form", claim_distribution: @claim_distribution %>
  <%# </div>
</div> %>

<%= render "remote_modal", title: "Add Claim Distribution" do %>
  <%= render "form", url: claim_distributions_path(v: @process_claim), meth: 'post' %>
<% end %>