<%# <div class="card">
  <h5 class="card-header">Edit claim distribution</h5>
  <div class="card-body"> %>
    <%#= render "form", claim_distribution: @claim_distribution %>
  <%# </div>
</div> %>

<%= render "remote_modal", title: "Edit benefit" do %>
  <%= render "form", url: claim_distribution_path(@claim_distribution), meth: 'patch' %>
<% end %>