
<%# <div class="card">
  <h5 class="card-header">Edit claim document</h5>
  <div class="card-body"> %>
    <%#= render "form", url: claim_documents_path(v: @process_claim), meth: 'post' %>
  <%# </div>
</div> %>


<%= render "remote_modal", title: "Add Claim Distribution" do %>
  <%= render "form", url: claim_documents_path(v: @process_claim), meth: 'post' %>
<% end %>