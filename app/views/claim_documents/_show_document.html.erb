<table class="table table-borderless table-hover">
  <thead>
    <%# <tr class="border-top border-bottom border-primary"> %>
      <%# <th class="text-primary">#</th> %>
      <%# <th class="text-primary">Document(s)</th>
      <th class="text-primary"></th>
    </tr> %>
  </thead>
  <tbody>
    <% @process_claim.claim_documents.each do |claim_document| %>
      <tr>
        <%# <th scope="row">= claim_document.id</th> %>
        <td><%= claim_document.document %></td>
        <td>
          <%#= link_to 'Edit', edit_claim_document_path(claim_document), class: 'btn btn-secondary btn-sm' %>
          <%#= link_to "Delete", claim_document, class: "btn btn-danger btn-sm", method: :delete, data: { "turbo-method": :delete, turbo_confirm: "Are you sure?" } %>
          <%= render 'claim_documents/attachment', cd: claim_document %>
          <%#= link_to '<i class="bi bi-pencil-square"></i>'.html_safe, rails_blob_path(claim_document.file,disposition: "attachment"), class: 'btn btn-secondary btn-sm' %>
          <%= link_to '<i class="bi bi-pencil-square"></i>'.html_safe, edit_claim_document_path(claim_document), class: 'btn btn-outline-primary btn-sm', data: { turbo_frame: "remote_modal" } %>
          <%= link_to '<i class="bi bi-trash3"></i>'.html_safe, claim_document, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
